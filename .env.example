# Copy to .env and fill in values. Do NOT commit .env

# 공공데이터포털 data.go.kr 인증키 (필수, API 호출 시)
API_PUBLIC_DATA_SERVICE_KEY=

# Spring profile: dev | prod
SPRING_PROFILES_ACTIVE=dev

# PostgreSQL (prod 프로파일 사용 시)
# SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5432/mediscan
# SPRING_DATASOURCE_USERNAME=mediscan
# SPRING_DATASOURCE_PASSWORD=

# 동기화 옵션
# API_SYNC_ON_STARTUP=true
# API_SYNC_ONLY_WHEN_EMPTY=true
